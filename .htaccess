RewriteEngine On

# Hilangkan .html dari URL
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^(.*)$ $1.html [L]

# Contoh: NETFLIX.html bisa diakses dengan /NETFLIX
